

function(conan_message MESSAGE_OUTPUT)
    if(NOT CONAN_CMAKE_SILENT_OUTPUT)
        message(${ARGV${0}})
    endif()
endfunction()


macro(conan_find_apple_frameworks FRAMEWORKS_FOUND FRAMEWORKS FRAMEWORKS_DIRS)
    if(APPLE)
        foreach(_FRAMEWORK ${FRAMEWORKS})
            # https://cmake.org/pipermail/cmake-developers/2017-August/030199.html
            find_library(CONAN_FRAMEWORK_${_FRAMEWORK}_FOUND NAME ${_FRAMEWORK} PATHS ${FRAMEWORKS_DIRS} CMAKE_FIND_ROOT_PATH_BOTH)
            if(CONAN_FRAMEWORK_${_FRAMEWORK}_FOUND)
                list(APPEND ${FRAMEWORKS_FOUND} ${CONAN_FRAMEWORK_${_FRAMEWORK}_FOUND})
            else()
                message(FATAL_ERROR "Framework library ${_FRAMEWORK} not found in paths: ${FRAMEWORKS_DIRS}")
            endif()
        endforeach()
    endif()
endmacro()


function(conan_package_library_targets libraries package_libdir deps out_libraries out_libraries_target build_type package_name)
    unset(_CONAN_ACTUAL_TARGETS CACHE)
    unset(_CONAN_FOUND_SYSTEM_LIBS CACHE)
    foreach(_LIBRARY_NAME ${libraries})
        find_library(CONAN_FOUND_LIBRARY NAME ${_LIBRARY_NAME} PATHS ${package_libdir}
                     NO_DEFAULT_PATH NO_CMAKE_FIND_ROOT_PATH)
        if(CONAN_FOUND_LIBRARY)
            conan_message(STATUS "Library ${_LIBRARY_NAME} found ${CONAN_FOUND_LIBRARY}")
            list(APPEND _out_libraries ${CONAN_FOUND_LIBRARY})
            if(NOT ${CMAKE_VERSION} VERSION_LESS "3.0")
                # Create a micro-target for each lib/a found
                string(REGEX REPLACE "[^A-Za-z0-9.+_-]" "_" _LIBRARY_NAME ${_LIBRARY_NAME})
                set(_LIB_NAME CONAN_LIB::${package_name}_${_LIBRARY_NAME}${build_type})
                if(NOT TARGET ${_LIB_NAME})
                    # Create a micro-target for each lib/a found
                    add_library(${_LIB_NAME} UNKNOWN IMPORTED)
                    set_target_properties(${_LIB_NAME} PROPERTIES IMPORTED_LOCATION ${CONAN_FOUND_LIBRARY})
                    set(_CONAN_ACTUAL_TARGETS ${_CONAN_ACTUAL_TARGETS} ${_LIB_NAME})
                else()
                    conan_message(STATUS "Skipping already existing target: ${_LIB_NAME}")
                endif()
                list(APPEND _out_libraries_target ${_LIB_NAME})
            endif()
            conan_message(STATUS "Found: ${CONAN_FOUND_LIBRARY}")
        else()
            conan_message(STATUS "Library ${_LIBRARY_NAME} not found in package, might be system one")
            list(APPEND _out_libraries_target ${_LIBRARY_NAME})
            list(APPEND _out_libraries ${_LIBRARY_NAME})
            set(_CONAN_FOUND_SYSTEM_LIBS "${_CONAN_FOUND_SYSTEM_LIBS};${_LIBRARY_NAME}")
        endif()
        unset(CONAN_FOUND_LIBRARY CACHE)
    endforeach()

    if(NOT ${CMAKE_VERSION} VERSION_LESS "3.0")
        # Add all dependencies to all targets
        string(REPLACE " " ";" deps_list "${deps}")
        foreach(_CONAN_ACTUAL_TARGET ${_CONAN_ACTUAL_TARGETS})
            set_property(TARGET ${_CONAN_ACTUAL_TARGET} PROPERTY INTERFACE_LINK_LIBRARIES "${_CONAN_FOUND_SYSTEM_LIBS};${deps_list}")
        endforeach()
    endif()

    set(${out_libraries} ${_out_libraries} PARENT_SCOPE)
    set(${out_libraries_target} ${_out_libraries_target} PARENT_SCOPE)
endfunction()


include(FindPackageHandleStandardArgs)

conan_message(STATUS "Conan: Using autogenerated Findstb.cmake")
# Global approach
set(stb_FOUND 1)
set(stb_VERSION "cci.20210713")

find_package_handle_standard_args(stb REQUIRED_VARS
                                  stb_VERSION VERSION_VAR stb_VERSION)
mark_as_advanced(stb_FOUND stb_VERSION)


set(stb_INCLUDE_DIRS "/Users/pierrejeannin/.conan/data/stb/cci.20210713/_/_/package/5ab84d6acfe1f23c4fae0ab88f26e3a396351ac9/include")
set(stb_INCLUDE_DIR "/Users/pierrejeannin/.conan/data/stb/cci.20210713/_/_/package/5ab84d6acfe1f23c4fae0ab88f26e3a396351ac9/include")
set(stb_INCLUDES "/Users/pierrejeannin/.conan/data/stb/cci.20210713/_/_/package/5ab84d6acfe1f23c4fae0ab88f26e3a396351ac9/include")
set(stb_RES_DIRS )
set(stb_DEFINITIONS "-DSTB_TEXTEDIT_KEYTYPE=unsigned")
set(stb_LINKER_FLAGS_LIST
        "$<$<STREQUAL:$<TARGET_PROPERTY:TYPE>,SHARED_LIBRARY>:>"
        "$<$<STREQUAL:$<TARGET_PROPERTY:TYPE>,MODULE_LIBRARY>:>"
        "$<$<STREQUAL:$<TARGET_PROPERTY:TYPE>,EXECUTABLE>:>"
)
set(stb_COMPILE_DEFINITIONS "STB_TEXTEDIT_KEYTYPE=unsigned")
set(stb_COMPILE_OPTIONS_LIST "" "")
set(stb_COMPILE_OPTIONS_C "")
set(stb_COMPILE_OPTIONS_CXX "")
set(stb_LIBRARIES_TARGETS "") # Will be filled later, if CMake 3
set(stb_LIBRARIES "") # Will be filled later
set(stb_LIBS "") # Same as stb_LIBRARIES
set(stb_SYSTEM_LIBS )
set(stb_FRAMEWORK_DIRS )
set(stb_FRAMEWORKS )
set(stb_FRAMEWORKS_FOUND "") # Will be filled later
set(stb_BUILD_MODULES_PATHS )

conan_find_apple_frameworks(stb_FRAMEWORKS_FOUND "${stb_FRAMEWORKS}" "${stb_FRAMEWORK_DIRS}")

mark_as_advanced(stb_INCLUDE_DIRS
                 stb_INCLUDE_DIR
                 stb_INCLUDES
                 stb_DEFINITIONS
                 stb_LINKER_FLAGS_LIST
                 stb_COMPILE_DEFINITIONS
                 stb_COMPILE_OPTIONS_LIST
                 stb_LIBRARIES
                 stb_LIBS
                 stb_LIBRARIES_TARGETS)

# Find the real .lib/.a and add them to stb_LIBS and stb_LIBRARY_LIST
set(stb_LIBRARY_LIST )
set(stb_LIB_DIRS )

# Gather all the libraries that should be linked to the targets (do not touch existing variables):
set(_stb_DEPENDENCIES "${stb_FRAMEWORKS_FOUND} ${stb_SYSTEM_LIBS} ")

conan_package_library_targets("${stb_LIBRARY_LIST}"  # libraries
                              "${stb_LIB_DIRS}"      # package_libdir
                              "${_stb_DEPENDENCIES}"  # deps
                              stb_LIBRARIES            # out_libraries
                              stb_LIBRARIES_TARGETS    # out_libraries_targets
                              ""                          # build_type
                              "stb")                                      # package_name

set(stb_LIBS ${stb_LIBRARIES})

foreach(_FRAMEWORK ${stb_FRAMEWORKS_FOUND})
    list(APPEND stb_LIBRARIES_TARGETS ${_FRAMEWORK})
    list(APPEND stb_LIBRARIES ${_FRAMEWORK})
endforeach()

foreach(_SYSTEM_LIB ${stb_SYSTEM_LIBS})
    list(APPEND stb_LIBRARIES_TARGETS ${_SYSTEM_LIB})
    list(APPEND stb_LIBRARIES ${_SYSTEM_LIB})
endforeach()

# We need to add our requirements too
set(stb_LIBRARIES_TARGETS "${stb_LIBRARIES_TARGETS};")
set(stb_LIBRARIES "${stb_LIBRARIES};")

set(CMAKE_MODULE_PATH "/Users/pierrejeannin/.conan/data/stb/cci.20210713/_/_/package/5ab84d6acfe1f23c4fae0ab88f26e3a396351ac9/" ${CMAKE_MODULE_PATH})
set(CMAKE_PREFIX_PATH "/Users/pierrejeannin/.conan/data/stb/cci.20210713/_/_/package/5ab84d6acfe1f23c4fae0ab88f26e3a396351ac9/" ${CMAKE_PREFIX_PATH})

if(NOT ${CMAKE_VERSION} VERSION_LESS "3.0")
    # Target approach
    if(NOT TARGET stb::stb)
        add_library(stb::stb INTERFACE IMPORTED)
        if(stb_INCLUDE_DIRS)
            set_target_properties(stb::stb PROPERTIES INTERFACE_INCLUDE_DIRECTORIES
                                  "${stb_INCLUDE_DIRS}")
        endif()
        set_property(TARGET stb::stb PROPERTY INTERFACE_LINK_LIBRARIES
                     "${stb_LIBRARIES_TARGETS};${stb_LINKER_FLAGS_LIST}")
        set_property(TARGET stb::stb PROPERTY INTERFACE_COMPILE_DEFINITIONS
                     ${stb_COMPILE_DEFINITIONS})
        set_property(TARGET stb::stb PROPERTY INTERFACE_COMPILE_OPTIONS
                     "${stb_COMPILE_OPTIONS_LIST}")
        
    endif()
endif()

foreach(_BUILD_MODULE_PATH ${stb_BUILD_MODULES_PATHS})
    include(${_BUILD_MODULE_PATH})
endforeach()
